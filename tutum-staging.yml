web-staging:
  image: 'tutum.co/borja/demo:staging'
  restart: always
  autoredeploy: true
  ports:
      - "80:80"
  links:
    - "redis-staging:redis"
  environment:
    - NAME=amigos
  tags:
    - staging

redis-staging:
  image: redis
  restart: always
  tags:
    - staging
